# ── Python ────────────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg-info/
.venv/
venv/
pip-log.txt

# Pyrogram session files — contain auth keys, never commit
*.session
*.session-journal

# Environment secrets — use .env.example instead
.env

# ── Node.js / Replit workspace (not part of the bot) ─────────────────────────
node_modules/
artifacts/
lib/
scripts/
pnpm-lock.yaml
pnpm-workspace.yaml
package.json
package-lock.json
tsconfig.json
tsconfig.base.json
*.tsbuildinfo
.npmrc

# ── Replit internal files ─────────────────────────────────────────────────────
.replit
.replitignore
replit.nix
pyproject.toml
uv.lock
.cache/
.local/
.upm/
.pythonlibs/
.agents/

# ── Misc ──────────────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
tmp_downloads/
*.log
